name: Visual regression

on:
  pull_request:
  workflow_dispatch:
    inputs:
      require_chromatic:
        description: Fail if CHROMATIC_PROJECT_TOKEN is not configured
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: visual-regression-${{ github.ref }}
  cancel-in-progress: true

jobs:
  storybook:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Storybook
        run: npm run build-storybook -- --output-dir storybook-static

      - name: Upload Storybook artifact
        uses: actions/upload-artifact@v4
        with:
          name: storybook-static
          path: storybook-static
          retention-days: 14

      - name: Chromatic visual regression
        if: ${{ secrets.CHROMATIC_PROJECT_TOKEN != '' }}
        uses: chromaui/action@v11
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          storybookBuildDir: storybook-static
          exitZeroOnChanges: false
          onlyChanged: true
          autoAcceptChanges: false

      - name: Chromatic setup notice
        if: ${{ secrets.CHROMATIC_PROJECT_TOKEN == '' }}
        run: |
          echo "::notice::CHROMATIC_PROJECT_TOKEN is not configured. Storybook build artifact was uploaded, but snapshot diff approval is inactive."
          {
            echo "## Visual regression setup required"
            echo ""
            echo "- Storybook build: completed"
            echo "- Chromatic upload: skipped because CHROMATIC_PROJECT_TOKEN is not configured"
            echo "- Required action: add the token and make this check required before broad production launch"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Require Chromatic setup
        if: ${{ inputs.require_chromatic && secrets.CHROMATIC_PROJECT_TOKEN == '' }}
        run: |
          echo "::error::Chromatic was required, but CHROMATIC_PROJECT_TOKEN is not configured."
          exit 1
