table:
  name: directory_import_quarantine
  schema: public
object_relationships:
  - name: preview
    using:
      foreign_key_constraint_on: preview_id
select_permissions:
  - role: admin
    permission:
      columns: "*"
      filter: {}
